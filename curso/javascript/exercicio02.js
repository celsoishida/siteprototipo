// Declaração das variáveis
const nome = "João";               // Tipo primitivo: string
const saudacao = "Bem-vindo(a), "; // Tipo primitivo: string

// Concatenação das strings
const mensagem = saudacao + nome;

// Exibição da mensagem com alert()
alert(mensagem);
