// Declaração das variáveis
const nome = "Celso Ishida";
const saudacao = "Bem-vindo(a)";

// Concatenação das strings
const mensagem = saudacao + " " + nome + "!";

// Exibição do resultado
alert(mensagem);
